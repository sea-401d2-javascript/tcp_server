"use strict";

const expect = require("chai").expect;
const createUniqueFileName = require(__dirname + "/../lib/createUniqueFileName.js");

describe("create unique file name", function() {
  it("should create unique name", function() {
    // expect(createUniqueFileName()).to.be.equal('');
  });
});