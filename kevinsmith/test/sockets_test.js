'use strict';

var chai = require('chai').expect;
var transform = require(__dirname + '/../sockets');

describe('Testing the sockets.js file'), function (){
  it('Should return a file'), function() {
    expect(true).to.equal(false);
  };
};
